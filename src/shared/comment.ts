export interface Comment{


    
    // Interface helps us to define a structure,
    //  or what we call as duck typing in TypeScript, 
    //  or structural subtyping in TypeScript. 
    //  So the interface allows us to define a structure,
    //  which defines a newly defined type that we can use to attribute to
    //  specific variables that we use within our TypeScript code here.

    rating:number;
    comment:string;
    author:string;
    date:string;




}